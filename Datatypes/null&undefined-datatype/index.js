let age; // declared but not defined

console.log(age , age + 1, `Age is ${age}`);

let name = null; //  set by intention

console.log(name + 1); // null will be considered as 0, so the answer will be 1;